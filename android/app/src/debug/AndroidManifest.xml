void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomeScreen(),
    );
  }
}

class HomeScreen extends StatefulWidget {
  @override
  _HomeScreenState createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  int messageCount = 0;
  Timer? timer;

  Future<void> sendToDiscordWebhook(String message) async {
    // Your sendToDiscordWebhook code here (same as before)

    if (response.statusCode == 204) {
      print('Message sent successfully to Discord webhook');
      setState(() {
        messageCount++;
      });
    } else {
      print('Failed to send message to Discord webhook. Status code: ${response.statusCode}');
      print('Response body: ${response.body}');
    }
  }

  @override
  void dispose() {
    timer?.cancel();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Discord Message Sender'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Text('Messages Sent: $messageCount'),
            ElevatedButton(
              onPressed: () {
                if (timer == null) {
                  // Start sending messages every 15 seconds
                  timer = Timer.periodic(Duration(seconds: 15), (timer) {
                    sendToDiscordWebhook("hkhttat now");
                  });
                } else {
                  // Stop sending messages
                  timer?.cancel();
                  timer = null;
                }
              },
              child: timer == null ? Text('Start Sending') : Text('Stop Sending'),
            ),
          ],
        ),
      ),
    );
  }
}
